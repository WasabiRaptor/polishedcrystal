	db "WILD PIGEON@" ; species name
	dw 6, 150 ; height, weight

	db "No matter where in the world it"
	next "goes, it knows where its nest"
	next "is, so it never gets separated"
	
	page "from its Trainer."
